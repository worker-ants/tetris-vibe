<!-- [worker-ants] generated by AI Agent (2026-03-14 15:03:08) -->
# Refinement Report — Review 2026-03-14_14-24-34

## P0 (즉시 수정)

### 1. `tick()` 등 게임 함수에 `!state.isStarted` 가드 명시 추가
- **상태**: 반영 완료
- **변경 파일**: `lib/tetris/gameEngine.ts`
- **내용**: `tick`, `moveLeft`, `moveRight`, `softDrop`, `hardDrop`, `rotate` 6개 함수 모두에 `|| !state.isStarted` 가드를 추가하여 `isPaused` 초기값에 대한 암묵적 의존 제거

### 2. `useEffect` tick 조건 및 의존 배열에 `state.isStarted` 추가
- **상태**: 반영 완료
- **변경 파일**: `app/components/TetrisGame.tsx`
- **내용**: gravity tick useEffect의 조건에 `|| !state.isStarted` 추가, 의존 배열에 `state.isStarted` 추가

### 3. `types.ts` 중복 `[worker-ants]` 마커 정리
- **상태**: 반영 완료
- **변경 파일**: `lib/tetris/types.ts`
- **내용**: 3중 중첩된 마커를 단일 쌍으로 정리 (hook이 자동 추가하는 마커는 제외)

---

## P1 (단기 개선)

### 4. `gameReducer` switch에 `default: return state` 추가
- **상태**: 반영 완료
- **변경 파일**: `lib/tetris/reducer.ts` (신규 — gameReducer 분리)
- **내용**: default 케이스 추가로 예상치 못한 액션 타입에 대한 안전 처리

### 5. Enter 키를 ControlsInfo에 반영
- **상태**: 반영 완료
- **변경 파일**: `app/components/ControlsInfo.tsx`, `app/components/TetrisGame.tsx`
- **내용**: ControlsInfo에 `Space / Enter → Start Game` 항목 추가, Space는 `Hard Drop`으로 분리 표시. 시작 화면 힌트 텍스트도 "Press Space or Enter to start"로 갱신

### 6. `GameState` 인터페이스에 상태 전이 JSDoc 추가
- **상태**: 반영 완료
- **변경 파일**: `lib/tetris/types.ts`
- **내용**: idle → running → paused → gameover 상태 전이를 JSDoc으로 문서화

### 7. RESTART 시 즉시 시작으로 동작 변경
- **상태**: 반영 완료
- **변경 파일**: `lib/tetris/reducer.ts`
- **내용**: RESTART 액션이 `initGame()` 후 `isStarted: true, isPaused: false`로 즉시 게임 시작 상태 반환. 이중 입력(재시작 → 다시 시작) 문제 해결

---

## P2 (중기 개선)

### 8. `vitest` 도입 및 `lib/tetris/` 순수 함수 단위 테스트 작성
- **상태**: 반영 완료
- **변경 파일**: `vitest.config.ts`, `lib/tetris/__tests__/gameEngine.test.ts`, `package.json`
- **내용**: vitest 설치, 순수 함수(initGame, startGame, togglePause, tick, moveLeft, moveRight, softDrop, hardDrop, rotate) 단위 테스트 작성

### 9. `gameReducer`를 `lib/tetris/reducer.ts`로 분리
- **상태**: 반영 완료
- **변경 파일**: `lib/tetris/reducer.ts` (신규), `app/components/TetrisGame.tsx`
- **내용**: gameReducer를 별도 모듈로 추출하여 TetrisGame.tsx의 컴포넌트 관심사 분리. RESTART 즉시 시작 로직과 default 케이스도 여기에 포함

---

## 추가 WARNING 반영

### Game Over 직후 Space 키 오발 트리거 방지
- **상태**: 반영 완료
- **변경 파일**: `app/components/TetrisGame.tsx`
- **내용**: Game Over 상태에서 Space 키를 restart 트리거에서 제거 (R 키만 허용). 하드 드롭 직후 의도치 않은 재시작 방지

### `startGame()`, `togglePause()` JSDoc 추가
- **상태**: 반영 완료
- **변경 파일**: `lib/tetris/gameEngine.ts`
- **내용**: 두 함수에 역할과 no-op 조건을 설명하는 JSDoc 추가

---

## 미반영 사항

### Pause/Restart 버튼 핸들러 중복 (P2 WARNING)
- **상태**: 미반영
- **사유**: 일시정지 오버레이 버튼과 사이드바 버튼은 서로 다른 UI 컨텍스트에서 표시되며, dispatch 호출 자체가 단순 인라인 표현식이므로 별도 추출의 실익이 낮음. 현 수준의 중복은 허용 범위로 판단

<!-- /[worker-ants] generated by AI Agent (2026-03-14 15:03:08) -->
