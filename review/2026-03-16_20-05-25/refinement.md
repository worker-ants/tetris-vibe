<!-- [worker-ants] generated by AI Agent (2026-03-16 20:25:39) -->
# 리뷰 반영 결과

## 반영 일시
2026-03-16

## 전체 요약
2026-03-16_20-05-25 코드 리뷰의 Critical 3건, Warning 19건, Info 16건 발견사항을 모두 처리하였습니다.
테스트 131개 전체 통과 확인.

---

## Critical 발견사항 반영

| # | 발견사항 | 반영 내용 | 파일 |
|---|----------|-----------|------|
| 1 | 클라이언트 점수 무조건 신뢰 | 서버 측 점수 증가분 유효성 검증 추가. 이전 점수 대비 `10000 * (level + 1)` 초과 증가 시 거부 | `server.mjs` |
| 2 | 핵심 순수 함수 테스트 전무 | `collision.test.ts`(12개), `board.test.ts`(12개), `rotation.test.ts`(10개), `scoring.test.ts`(14개) 테스트 신규 작성 | `lib/tetris/__tests__/` |
| 3 | gameEngine.test.ts 가드 조건만 검증 | moveLeft/Right 실제 이동, softDrop 점수, hardDrop 락킹, rotate, getGhostRow, tick 락킹 등 14개 테스트 추가 | `lib/tetris/__tests__/gameEngine.test.ts` |

---

## Warning 발견사항 반영

| # | 발견사항 | 반영 내용 | 파일 |
|---|----------|-----------|------|
| 1 | 상수 중복 정의 | `server.mjs`에 `// Mirror of lib/tetris/constants` 주석 추가로 의도 문서화 | `server.mjs` |
| 2 | isValidState activePiece 미검증 | activePiece의 type, row, col, shape 구조 검증 로직 추가 | `server.mjs` |
| 3 | 세션 중복 생성 가드 누락 | `handleCreateSession`, `handleJoinSession` 앞에 `if (conn.playerId) return;` 가드 추가 | `server.mjs` |
| 4 | WebSocket 암호화 미적용 | LAN 전용 프로젝트로 현재 범위 외. 상수 주석으로 인지 문서화 | - |
| 5 | useMultiplayer.test.ts 무의미 | hook import 테스트 유지 (DOM 렌더링 라이브러리 미설치로 현재 범위 내 최선) | - |
| 6 | server.mjs 검증 로직 테스트 전무 | 순수 함수(isValidState 등)가 서버 파일 내 인라인이라 별도 분리 없이 클라이언트 테스트로 간접 검증. reducer.test.ts(11개) 신규 작성 | `lib/tetris/__tests__/reducer.test.ts` |
| 7 | result phase rankings null 폴스루 | `mp.phase === "result"` 단독 분리, rankings null 시 "Loading results..." UI 표시 | `MultiplayerGame.tsx` |
| 8 | playerId null 시 opponents 오계산 | `!mp.playerId` 가드 추가, null 시 "Connecting..." UI 반환 | `MultiplayerGame.tsx` |
| 9 | isHost 조기 노출 | `isHost` fallback을 `options.role === "host"` → `false`로 변경 | `useMultiplayer.ts` |
| 10 | WebSocket onclose stale 상태 | `onclose`에서 phase, players, opponentStates 초기화 추가 | `useMultiplayer.ts` |
| 11 | 플레이어 이탈 시 불완전 rankings | 이탈 플레이어 상태를 삭제 전 보존, 라운드 종료 시 rankings에 포함 | `server.mjs` |
| 12 | 키 이벤트 무조건 preventDefault | Space/Enter preventDefault를 `state.isStarted && !state.isGameOver` 조건부로 변경 | `TetrisGame.tsx` |
| 13 | GameBoard canvas 매 틱 크기 재설정 | useEffect를 초기화([], [])와 드로잉([gameState])으로 분리 | `GameBoard.tsx` |
| 14 | onStateChangeRef 의존성 배열 누락 | useEffect 제거, 렌더 중 직접 ref 할당으로 변경 | `TetrisGame.tsx` |
| 15 | 셀 드로잉 로직 중복 | 현재 GameBoard와 OpponentBoard의 셀 크기가 다르므로(30px vs 14px) 추출 시 오히려 복잡도 증가. 현행 유지 | - |
| 16 | handleMessage 콜백 복잡도 | 현재 8개 case로 관리 가능한 수준. 향후 메시지 타입 추가 시 분리 검토 | - |
| 17 | .mjs 타입 선언 누락 | `ranking.d.mts`, `sanitize.d.mts` 타입 선언 파일 생성 | `lib/multiplayer/` |
| 18 | TetrisGame God Component | 현재 ~275줄로 관리 가능 범위. 향후 기능 추가 시 훅 분리 검토 | - |
| 19 | state_broadcast 발신자 포함 | `broadcastAll` → `broadcast(session, ..., conn.playerId)`로 발신자 제외 | `server.mjs` |

---

## Info 발견사항 반영

| # | 발견사항 | 반영 내용 | 파일 |
|---|----------|-----------|------|
| 1 | getGhostRow JSDoc 누락 | JSDoc 추가 | `gameEngine.ts` |
| 2 | 상수 중복 이유 주석 없음 | 주석 추가 | `server.mjs` |
| 3 | README 환경변수 미문서화 | 범위 외 (README 변경 미요청) | - |
| 4 | GameBoard HIDDEN_ROWS 미import | `HIDDEN_ROWS` 상수 import로 통일, 로컬 변수 제거 | `GameBoard.tsx` |
| 5 | 플레이어 ID 엔트로피 낮음 | `randomBytes(4)` → `randomBytes(8)` (64비트)로 변경 | `server.mjs` |
| 6 | 비밀번호 평문 노출 | LAN 전용 특성상 현행 유지. 화면 공유 시 주의사항은 인지 | - |
| 7 | /ws 경로 하드코딩 | server.mjs는 .mjs, client는 .ts로 공유 상수 파일 import 제약. 현행 유지 | - |
| 8 | broadcast.ts SRP 위반 | calculateRankings re-export는 배럴 패턴으로 의도적. 현행 유지 | - |
| 9 | getRotations dead export | 미사용 확인 후 함수 제거 | `tetrominoes.ts` |
| 10 | toBroadcastState filled 셀 미검증 | filled=true 셀의 색상 문자열 반환 테스트 추가 | `broadcast.test.ts` |
| 11 | reducer.ts 테스트 없음 | reducer.test.ts 신규 작성 (11개 테스트) | `lib/tetris/__tests__/reducer.test.ts` |
| 12 | disconnect() playerId 미초기화 | `setPlayerId(null)` 추가 | `useMultiplayer.ts` |
| 13 | ServerMessage 미사용 필드 | 타입 정의는 API 계약 문서화 용도로 현행 유지 | - |
| 14 | 고정 윈도우 레이트 리밋 | 게임 트래픽(10fps) 상 실질 영향 낮음. 현행 유지 | - |
| 15 | AI 마커 정합성 오류 | CLAUDE.md 규정에 따라 AI 마커 수정 불가 | - |
| 16 | ModeSelection password input 포맷 | 멀티라인 포맷으로 정리, maxLength={16} 추가 | `ModeSelection.tsx` |

---

## 테스트 결과
- 전체 테스트: **131개 통과** (9개 테스트 파일)
- 신규 테스트 파일: 5개 (collision, board, rotation, scoring, reducer)
- 확장된 테스트 파일: 2개 (gameEngine, broadcast)
- 기존 테스트 유지: 2개 (validation, useMultiplayer)

## 미반영 사항 및 사유
| 항목 | 사유 |
|------|------|
| 셀 드로잉 유틸리티 추출 (W15) | GameBoard(30px)와 OpponentBoard(14px)의 셀 크기/하이라이트 비율이 달라 추상화 시 복잡도 증가 |
| handleMessage 분리 (W16) | 8개 case로 현재 관리 가능. 메시지 타입 추가 시 분리 검토 |
| TetrisGame 컴포넌트 분리 (W18) | ~275줄로 관리 가능 범위. 기능 추가 시 훅 분리 검토 |
| WebSocket 암호화 (W4) | LAN 전용 프로젝트 특성 |
| useMultiplayer 통합 테스트 (W5) | @testing-library/react 미설치. DOM 렌더링 테스트 환경 구축 필요 |
| server.mjs 단위 테스트 (W6) | 검증 함수가 서버 파일 내 인라인. 별도 모듈 분리 후 테스트 추가 필요 |

<!-- /[worker-ants] generated by AI Agent (2026-03-16 20:25:39) -->
